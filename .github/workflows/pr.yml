name: PR

on:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/lua-tools
      - name: Lint
        run: make lint
      - name: Check formatting
        run: make check
      # Building every zip proves the .toc file lists still resolve and
      # packaging did not break, without uploading anywhere.
      - name: Package every artifact
        run: |
          make package
          make package-soundscaper
          make package-decorspendwatch
          make package-compassbar
      - name: Upload the zips for inspection
        uses: actions/upload-artifact@v4
        with:
          name: packages
          path: "*.zip"
